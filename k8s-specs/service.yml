apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: e-commerce
spec:
  selector:
    app: e-commerce
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
